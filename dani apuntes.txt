<!-- <% if (locals.errors) { %>
        <% for( let oneError in errors ) { %>
            <p> <%= errors[oneError].msg %>  </p>
            <% } %>
    <% } %>-->


esto no me anda:
no estan en rojo las letras de las advertencias:
<div class="text-danger">
</div>

no me aparece en rojo el marco del campo a completar:
class="reg <%= locals.errors && errors.apellido ? 'is-invalid' : null %>" 

exactamente este pinche ejsout no anda
<%= locals.errors && errors.apellido ? 'is-invalid' : null %>

/**jajaja esto no sirve fue mi intento de hacer un confirm password:
esto noc si anda
<%= locals.oldData && oldData.otraPassword === oldData.password ? 'locals.errors' : null %> 
**/

